const solution = require('./kobe.js');

describe('', () => {
  it('should be ', () => {
    expect(solution())
  });
  
  it('should be ', () => {
    expect(solution())
  });
  
  it('should be ', () => {
    expect(solution())
  });
})